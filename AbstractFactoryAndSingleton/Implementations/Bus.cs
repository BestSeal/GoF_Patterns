using AbstractFactoryAndSingleton.BaseClasses;

namespace AbstractFactoryAndSingleton.Implementations
{
    public class Bus : Vehicle
    {
        public Bus(int capacity, int price) : base(capacity, price){}
        
        public o
    }
}